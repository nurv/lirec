/*
#include <juce_amalgamated.h>



class LogComponent  : public Component, public ButtonListener
{
public:

    LogComponent ();
    ~LogComponent();

    void resized();
    void buttonClicked (Button* buttonThatWasClicked);

    juce_UseDebuggingNewOperator

private:

    Label* helloWorldLabel;
    TextButton* quitButton;

    LogComponent (const LogComponent&);
    const LogComponent& operator= (const LogComponent&);
};

*/
